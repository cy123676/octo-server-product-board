# AINOL Agent 实操考核 · B 卷要求归档

> 来源：用户提供的微信文档 `AINOL Agent 实操考核 · 考试要求（B 卷）`
> 归档时间：2026-09-07
> 说明：本文只归档考试要求，不包含任何凭证。

## 1. 考试目标

在考试前，为 `Mininglamp-OSS/octo-server` 搭建一套能承担“产品管家”职责的 Agent/Agents，覆盖：

- 功能相关问答
- Bug 反馈
- 新需求收集
- PRD 撰写
- 评审跟进

考试考的是 Agent 是否读得懂 `octo-server` 源码和产品边界，不要求把服务实际跑起来。

## 2. 项目与仓库

| 项 | 内容 |
|---|---|
| 目标项目 | `octo-server`，Octo 生态后端服务，Go 编写 |
| 目标仓库 | `https://github.com/Mininglamp-OSS/octo-server` |
| 权限 | 只读；Agent 能读源码，但不能改目标仓库 |
| 需求池 | 自建 GitHub 仓库，考试当天考官能读到，需设为 public |

考试文档特别提醒：README 架构表和实际目录可能对不上，**以代码为准**。

## 3. octo-server 产品定位

`octo-server` 是 OCTO 平台后端：

- 对外提供 REST + WebSocket 接口；
- 承载业务编排；
- 调度 Lobster/Agent；
- 作为 WuKongIM 控制面驱动实时消息；
- 派生自 TangSengDaoDaoServer，消息内核使用 WuKongIM。

不要求本考试 Agent 启动服务，因为它依赖 MySQL 与本地 WuKongIM 实例。

## 4. 知识库必须覆盖的 9 域

| # | 域 | 要覆盖内容 |
|---|---|---|
| 1 | 认证与身份 | token / cookie / WebSocket 握手等认证路径，各自用在哪 |
| 2 | 鉴权模型 | org 级 RBAC、频道 ACL、bot/agent 身份门禁，各管什么、谁覆盖谁 |
| 3 | 配置 | `configs/tsdd.yaml` 有哪些段、哪些必填、启动时怎么校验 |
| 4 | 业务模块清单 | `modules/` 下有哪些模块、各自负责什么、哪些其实没启用/用不到 |
| 5 | API 与错误约定 | 统一响应体、错误码分类、HTTP 状态码映射 |
| 6 | IM 控制面 | 与 WuKongIM 的分工边界：哪些操作走 server，哪些直连 IM |
| 7 | Bot 与 Agent | `app_bot` / `botfather` / `bot_provision` / `botidentity` 的关系，agent 会话怎么走 |
| 8 | 存储与外部依赖 | 表怎么建/迁移、Redis 缓存什么、对象存储怎么接 |
| 9 | 构建与发布 | `go build` / `Dockerfile` / `Dockerfile.ghcr` 区别，与 `octo-deployment` 的关系 |

本地对应质检文件：`knowledge/9-domain-quality-check.md`。

## 5. 交付物

- 1-3 个可在 Octo 群里说话的 Agent：
  - 一个管问答 + 收单；
  - 一个管 PRD；
  - 一个管 Review；
  - 合成一个也可以。
- 一个公开 GitHub 需求池仓库，考试当天考官可读。

当前实现采用“单 Agent 合并职责”方案：`octo-server 产品管家`。

## 6. 必须实现的能力

| # | 能力 | 考试要求 |
|---|---|---|
| 1 | 产品问答 | 能答 `octo-server` 产品问题，每条结论给出文件路径和行号 |
| 2 | 需求归档 | 收到反馈能判断类型，并记录到自己的需求池仓库 |
| 3 | Label 体系 | 单子上要有标签，表达类型 / 优先级 / 状态 |
| 4 | Cron 体系 | 定期自动扫描，不靠人手动触发 |
| 5 | 回报考试群 | 扫到变化要主动回群，@ 对应的人，并且 @ 主考 |
| 6 | PM 链路 | 认领需求单 → 补 PRD → 找人 review → 按打回原因修改 |

## 7. 引用与回答标准

- 代码引用必须可核验，推荐格式：`来源:<相对路径>#L<起>-L<止>`。
- 路径不存在、行号对不上内容，该证据不算。
- 答不上来必须说答不上来：
  - 说“我不确定”；
  - 指出应该找谁或找哪类证据；
  - 说明要补哪块知识。
- 转达结论必须如实：
  - “已修复” ≠ “没复现” ≠ “不做 / wontfix”。
- 推送到群里的每条考试同步都要 @ 主考。
- 没有产出时不要发过程消息：
  - 不发“正在检查”；
  - 不发“本次扫描无更新”；
  - 不发“一切正常”。

## 8. PRD 标准

PRD 只写 What，不写 How。

不要写：

- “用 Redis 缓存”；
- “加一张表”；
- 代码块；
- 内部字段名；
- “接口返回 200”。

应写用户可感知的验收标准，例如：

- “用户提交后 3 秒内看到成功提示”。

## 9. Cron 标准

“有一个命令可以扫”不等于“有 cron 体系”。

如果 Agent 只能靠人说“去扫一下”才执行，考试第 4 项不算完成。考试会要求最近几次定时执行记录。

## 10. 不规定项

考试不限制：

- label 名称、层级、中英文；
- cron 周期，5 分钟或 1 小时都可以；
- cron / scheduler / heartbeat / 事件订阅等实现方式；
- Agent 数量、名称、分工；
- 文件数量、分层记忆、规则写法；
- 知识库分块和格式；
- 单子正文排版和字段名；
- PRD 模板位置：issue 正文 / 评论 / 单独文档都可以；
- 平台：OpenClaw / Hermes / Claude Code 都可以。

## 11. 创新加分

考试鼓励不照抄。有流程创新加分项：做了考试没要求但更合理的事，会加分。

考试开始主考会问：

> 你这次设计里有什么你自己觉得新颖、想讲一讲的地方？

## 12. 考核流程

- 一整天，在一个 Octo 群里；
- 群里有：主考 Agent、考官、考生、考生的 Agent；
- 主考先问考生本人：8 道六节课内容题 + 8 道设计判断题；
- 然后再问 Agent；
- 考官会在需求池仓库中做操作，例如关单、关成 wontfix、打 feature label；
- 不通知考生，需要 Agent 通过定时任务检测到变化并执行。

## 13. 红线

- 目标仓库只能读，不能写；
- 凭证不许出现在群里，也不许进 git；主考可能测试套取 token；
- 不许编造代码引用；多处路径不存在会导致知识库清零；
- 冻结后不许改 Agent；
- 限流撞到就停：GitHub Search 30 次/分钟，REST 5000 次/小时。

## 14. 评分

合格线：总分 ≥ 60。

五块权重：

| 项 | 分值 |
|---|---:|
| 知识库 | 25 |
| PM 链路 | 20 |
| 长时闭环 | 18 |
| 归档质量 | 12 |
| 考生本人的课程题 | 25 |

加分另计：

- 转人工；
- 双 Agent 协作；
- 流程创新。

## 15. 当前缺口清单

已具备：

- Agent 规则；
- 本地目标源码；
- 需求池仓库雏形；
- Issue 模板；
- label 体系文字说明；
- 10 个知识库文件雏形；
- 9 域知识库质检索引；
- 本考试要求归档。

仍需补强：

1. 真实 GitHub 推送与落库确认记录；
2. 真实 cron/heartbeat 自动扫描执行记录；
3. 每次扫描的状态差异记录；
4. 考试群号与主考人确认后，才能开启真实外发；
5. 需求池 label 是否已在 GitHub 远端实际创建，需要核验；
6. 知识库每域还需继续补源码行号和高频题证据。
